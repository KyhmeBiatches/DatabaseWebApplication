@model IEnumerable<DatabaseWebApplication.QuestionSettings>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QuestionTrigger.id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QuestionType.typename)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.a_amount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QuestionTrigger.id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QuestionType.typename)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.a_amount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
